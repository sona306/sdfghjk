projection;
strict ( 2 );
use side effects;
use draft;
define behavior for ZC_EMPLOYEE_DETAILS //alias <alias_name>
{
  use create;
  use update;
  use delete;
  use action Prepare;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use association _leaves { create;with draft; }
}

define behavior for ZC_LEAVES //alias <alias_name>
{
  use update;
  use delete;
  use action Approve;
  use action Reject;
  use association _employee;

}